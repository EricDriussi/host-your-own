stages:
  - run

run-job:
  stage: run
  image: debian:latest
  script:
    - apt-get update && apt-get install -y curl
    # Entrypoint for setup
    - curl -sSL https://gitlab.com/ericdriussi/host-your-own/-/raw/ci/init.sh | bash -s -- ci
