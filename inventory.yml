---
all:
  hosts:
    ansible:
      ansible_host: "{{ ip }}"
      ansible_user: "{{ username }}"
    root:
      ansible_host: "{{ ip }}"
      ansible_user: "root"

  vars:
    username: ansible
    backup_dir: "/home/{{ username }}/backups"
    service:
      cloud:
        subdomain: "cloud"
        port: 81
      vault:
        subdomain: "vault"
        port: 82
      searx:
        subdomain: "searx"
        port: 83
    ports:
      ssh: 22
      http: 80
      https: 443
