---
- name: Run container
  docker_container:
    name: searx
    image: searxng/searxng:latest
    env:
      INSTANCE_NAME: "SearX!"
    detach: yes
    state: started
    volumes:
      - "/home/{{ username }}/searxng:/etc/searxng"
    ports:
      - "{{ ports.searx }}:8080"
