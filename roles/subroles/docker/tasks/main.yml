---
- name: Install docker and compose
  package:
    name:
      - docker
      - docker-compose
    state: latest

- name: Start and enable docker
  systemd:
    name: docker
    daemon_reload: true
    state: started
    enabled: true
