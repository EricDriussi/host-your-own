---
- name: Install snap
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - snapd

- name: Install snap core and nextcloud
  snap:
    name:
      - core
      - nextcloud
    state: present
  register: install
